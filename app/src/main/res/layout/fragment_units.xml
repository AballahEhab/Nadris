<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="adapter" type="com.example.android.nadris.ui.studentActivity.units.UnitItemAdapter"/>

        <variable
            name="viewmodel"
            type="com.example.android.nadris.ui.studentActivity.units.UnitsViewModel"/>
    </data>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.studentActivity.units.UnitsFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/units_rv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adapter="@{adapter}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_expandable_unit"/>
    </FrameLayout>
</layout>